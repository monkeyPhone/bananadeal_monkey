import 'package:flutter/material.dart';
import '../../../../../../css/size.dart';

class EtcPad extends StatelessWidget {
  const EtcPad({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: WidgetSize(context).sizedBox10,
    );
  }
}
