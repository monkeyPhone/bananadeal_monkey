name: data_provider
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.6.1

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^9.0.0


  models:
    path: ../../global/models
  common_provider:
    path: ../../common/common_provider
  permission_api:
    path: ../permission_api
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
