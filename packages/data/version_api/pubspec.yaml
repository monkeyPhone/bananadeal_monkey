name: version_api
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  package_info_plus: ^8.1.2

  common_network:
    path: ../../common/common_network
  common_url:
    path:  ../../common/common_url
  common_cache:
    path: ../../common/common_cache

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
